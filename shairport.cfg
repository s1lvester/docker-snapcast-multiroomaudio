general = {
    name = "MultiRoomAudio"
    output_backend = "pipe"
    interpolation = "soxr"
    mdns_backend = "avahi"

}

pipe = {
    name = "/tmp/snapfifo"
    audio_backend_latency_offset = -4410
}
